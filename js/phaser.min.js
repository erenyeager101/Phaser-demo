import Phaser from 'phaser';
function create(){
    this.add.text(50, 100, "fkk this", { font: "40px Times New Roman", fill: "#ffa0d0"});

  this.add.text(130, 300, "by life", { font: "20px Times New Roman", fill: "#ffa0d0"});

  this.add.circle(200, 200, 50, 0x00ff00);
  ball = this.physics.add.image(200, 200, 'ball');
  sky = this.add.image(200, 200, 'logo');


    
}
function preload(){
    this.load.image('logo', 'assets/logo.png');
    game.load.image('ball', 'assets/ball.png');
}





const config={
    type: Phaser.AUTO,
    width: 450,
	height: 600,
	backgroundColor: "#5f2a55",
	scene: {
    create
	}
};

const Game = new Phaser.Game(config);